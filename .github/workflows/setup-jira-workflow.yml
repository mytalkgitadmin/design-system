name: Setup Jira Workflow

on:
  workflow_dispatch:

jobs:
  setup-workflow:
    name: Setup Jira Workflow Configuration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch Jira workflow from project
        id: fetch-transitions
        run: |
          PROJECT_KEY="${{ secrets.JIRA_PROJECT }}"

          echo "🔍 프로젝트 ${PROJECT_KEY}의 워크플로우 조회 중..."

          # 프로젝트의 모든 상태 조회
          STATUSES=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/3/project/${PROJECT_KEY}/statuses")

          # 에러 체크
          if echo "$PROJECT_INFO" | jq -e '.errorMessages' > /dev/null 2>&1; then
            echo "❌ Jira API 에러:"
            echo "$PROJECT_INFO" | jq -r '.errorMessages[]'
            exit 1
          fi

          # Task 타입의 상태들 추출 (가장 일반적)
          TASK_STATUSES=$(echo "$STATUSES" | jq '[.[] | select(.name == "Task" or .name == "작업" or .id == "10001") | .statuses[]] | unique')

          # 상태가 없으면 첫 번째 issue type 사용
          if [ "$TASK_STATUSES" = "[]" ] || [ "$TASK_STATUSES" = "null" ]; then
            echo "⚠️  Task 타입을 찾을 수 없습니다. 첫 번째 issue type 사용..."
            TASK_STATUSES=$(echo "$STATUSES" | jq '[.[0].statuses[]] | unique')
          fi

          echo "📋 사용 가능한 상태 목록:"
          echo "$TASK_STATUSES" | jq -r '.[] | "  \(.name) (ID: \(.id))"'

          # 샘플 이슈로 실제 transition 조회 (상태 ID를 알기 위함)
          # 프로젝트에서 최근 이슈 하나 가져오기
          echo ""
          echo "🔄 Transition 매핑 생성 중..."

          # 각 상태의 ID 추출
          TODO_ID=$(echo "$STATUSES" | jq -r '.[] | select(.name == "해야 할 일" or .name == "To Do" or .name == "Open" or .name == "미해결") | .id' | head -n 1)
          INPROGRESS_ID=$(echo "$STATUSES" | jq -r '.[] | select(.name == "진행 중" or .name == "In Progress") | .id' | head -n 1)
          DONE_ID=$(echo "$STATUSES" | jq -r '.[] | select(.name == "완료" or .name == "Done" or .name == "해결됨" or .name == "종료") | .id' | head -n 1)
          REVIEW_ID=$(echo "$STATUSES" | jq -r '.[] | select((.name | contains("리뷰")) or (.name | contains("Review"))) | .id' | head -n 1)

          echo "🎯 매핑된 상태:"
          [ -n "$TODO_ID" ] && echo "  시작 상태: $TODO_ID"
          [ -n "$INPROGRESS_ID" ] && echo "  진행 중: $INPROGRESS_ID"
          [ -n "$DONE_ID" ] && echo "  완료: $DONE_ID"
          [ -n "$REVIEW_ID" ] && echo "  리뷰: $REVIEW_ID"

          # 4. 샘플 이슈 조회
          echo ""
          echo "🔍 샘플 이슈 조회 중..."
          SEARCH_RESULT=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/search?jql=project=${PROJECT_KEY}&maxResults=1")

          SAMPLE_ISSUE=$(echo "$SEARCH_RESULT" | jq -r '.issues[0].key')

          if [ -z "$SAMPLE_ISSUE" ] || [ "$SAMPLE_ISSUE" = "null" ]; then
            echo "⚠️  프로젝트에 이슈가 없습니다. 임시 이슈를 생성합니다..."
            
            # 임시 이슈 생성
            CREATE_RESPONSE=$(curl -s -X POST \
              -H "Content-Type: application/json" \
              -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
              -d "{
                \"fields\": {
                  \"project\": {\"key\": \"${PROJECT_KEY}\"},
                  \"summary\": \"[Temporary] Workflow Configuration\",
                  \"description\": \"이 이슈는 workflow 설정을 위해 자동 생성되었습니다. 곧 삭제될 예정입니다.\",
                  \"issuetype\": {\"id\": \"$(echo "$TASK_STATUSES" | jq -r '.[0].id // "10001"')\"}
                }
              }" \
              "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue")
            
            SAMPLE_ISSUE=$(echo "$CREATE_RESPONSE" | jq -r '.key')
            
            if [ -z "$SAMPLE_ISSUE" ] || [ "$SAMPLE_ISSUE" = "null" ]; then
              echo "❌ 임시 이슈 생성 실패:"
              echo "$CREATE_RESPONSE" | jq '.'
              exit 1
            fi
            
            echo "✅ 임시 이슈 생성됨: $SAMPLE_ISSUE"
            SHOULD_DELETE_ISSUE=true
          else
            SHOULD_DELETE_ISSUE=false
          fi

          echo "✅ 샘플 이슈 발견: $SAMPLE_ISSUE"
          echo ""

          # 실제 transition 조회
          TRANSITIONS=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/${SAMPLE_ISSUE}/transitions")

          echo "📋 사용 가능한 Transition 목록:"
          echo "$TRANSITIONS" | jq -r '.transitions[] | "  ID: \(.id) | \(.name) → \(.to.name)"'

          # transitions 데이터를 output으로 저장 (배열만 추출)
          echo ""
          echo "💾 Output 저장 중..."
          TRANSITIONS_ARRAY=$(echo "$TRANSITIONS" | jq '.transitions')

          echo "DEBUG: Transitions array:"
          echo "$TRANSITIONS_ARRAY"
          echo "DEBUG: Array length: $(echo "$TRANSITIONS_ARRAY" | wc -c)"

          echo "transitions<<EOF" >> $GITHUB_OUTPUT
          echo "$TRANSITIONS_ARRAY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          echo "sample_issue=$SAMPLE_ISSUE" >> $GITHUB_OUTPUT
          echo "should_delete=$SHOULD_DELETE_ISSUE" >> $GITHUB_OUTPUT

          echo "✅ Output 저장 완료"

      - name: Auto-detect transitions by moving through states
        id: auto-detect
        run: |
          SAMPLE_ISSUE="${{ steps.fetch-transitions.outputs.sample_issue }}"
          SHOULD_DELETE="${{ steps.fetch-transitions.outputs.should_delete }}"

          echo "🔍 단계별 transition 조회 시작..."
          echo "  샘플 이슈: $SAMPLE_ISSUE"
          echo ""

          # 초기 transitions (전체) 수집
          INITIAL_TRANSITIONS=$(cat <<'TRANSITIONS_EOF'
          ${{ steps.fetch-transitions.outputs.transitions }}
          TRANSITIONS_EOF
          )

          # 1단계: 초기 상태에서 "진행 중"으로 가는 transition 찾기
          echo "📍 1단계: 작업 시작 transition 찾기"
          CURRENT_STATUS=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/${SAMPLE_ISSUE}" | jq -r '.fields.status.name')

          echo "  현재 상태: $CURRENT_STATUS"

          AVAILABLE_TRANS=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/${SAMPLE_ISSUE}/transitions")

          echo "  사용 가능한 transitions:"
          echo "$AVAILABLE_TRANS" | jq -r '.transitions[] | "    - \(.name) → \(.to.name) (ID: \(.id))"'

          # Transition 이름으로 찾기 (우선순위: 이름 > 목표 상태)
          START_ID=$(echo "$AVAILABLE_TRANS" | jq -r '.transitions[] | select(
            (.name | ascii_downcase) | contains("start") or
            (.name | ascii_downcase) | contains("progress") or
            (.name | ascii_downcase) | contains("시작") or
            (.name | ascii_downcase) | contains("진행") or
            (.to.name | ascii_downcase) == "in progress" or
            (.to.name | ascii_downcase) == "진행 중"
          ) | .id' | head -n 1)

          if [ -n "$START_ID" ] && [ "$START_ID" != "null" ]; then
            START_STATUS=$(echo "$AVAILABLE_TRANS" | jq -r ".transitions[] | select(.id == \"$START_ID\") | .to.name")
            START_TRANS_NAME=$(echo "$AVAILABLE_TRANS" | jq -r ".transitions[] | select(.id == \"$START_ID\") | .name")
            echo "  ✅ 작업 시작: $CURRENT_STATUS → $START_STATUS (Transition: $START_TRANS_NAME, ID: $START_ID)"
            
            # "진행 중" 상태로 이동
            curl -s -X POST \
              -H "Content-Type: application/json" \
              -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
              -d "{\"transition\": {\"id\": \"${START_ID}\"}}" \
              "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/${SAMPLE_ISSUE}/transitions" > /dev/null
            
            sleep 1
          else
            echo "  ⚠️  작업 시작 transition을 찾을 수 없습니다"
            START_STATUS=""
          fi
          echo ""

          # 2단계: "진행 중" 상태에서 "완료"로 가는 transition 찾기
          echo "📍 2단계: 작업 완료 transition 찾기"
          CURRENT_STATUS=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/${SAMPLE_ISSUE}" | jq -r '.fields.status.name')

          echo "  현재 상태: $CURRENT_STATUS"

          PROGRESS_TRANS=$(curl -s -X GET \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/${SAMPLE_ISSUE}/transitions")

          echo "  사용 가능한 transitions:"
          echo "$PROGRESS_TRANS" | jq -r '.transitions[] | "    - \(.name) → \(.to.name) (ID: \(.id))"'

          # Transition 이름으로 찾기 (우선순위: 이름 > 목표 상태)
          END_ID=$(echo "$PROGRESS_TRANS" | jq -r '.transitions[] | select(
            (.name | ascii_downcase) | contains("resolve") or
            (.name | ascii_downcase) | contains("done") or
            (.name | ascii_downcase) | contains("close") or
            (.name | ascii_downcase) | contains("해결") or
            (.name | ascii_downcase) | contains("완료") or
            (.name | ascii_downcase) | contains("종료") or
            (.to.name | ascii_downcase) == "done" or
            (.to.name | ascii_downcase) == "완료" or
            (.to.name | ascii_downcase) == "해결됨" or
            (.to.name | ascii_downcase) == "종료"
          ) | .id' | head -n 1)

          if [ -n "$END_ID" ] && [ "$END_ID" != "null" ]; then
            END_STATUS=$(echo "$PROGRESS_TRANS" | jq -r ".transitions[] | select(.id == \"$END_ID\") | .to.name")
            END_TRANS_NAME=$(echo "$PROGRESS_TRANS" | jq -r ".transitions[] | select(.id == \"$END_ID\") | .name")
            echo "  ✅ 작업 완료: $CURRENT_STATUS → $END_STATUS (Transition: $END_TRANS_NAME, ID: $END_ID)"
          else
            echo "  ⚠️  작업 완료 transition을 찾을 수 없습니다"
            END_STATUS=""
          fi
          echo ""

          # 3단계: 리뷰 transition 찾기 (초기 transitions에서)
          echo "📍 3단계: 리뷰 transition 찾기"
          REVIEW_ID=$(echo "$INITIAL_TRANSITIONS" | jq -r '.[] | select(
            (.to.name | ascii_downcase) | contains("review") or
            (.to.name | ascii_downcase) | contains("리뷰")
          ) | .id' | head -n 1)

          if [ -n "$REVIEW_ID" ] && [ "$REVIEW_ID" != "null" ]; then
            REVIEW_STATUS=$(echo "$INITIAL_TRANSITIONS" | jq -r ".[] | select(.id == \"$REVIEW_ID\") | .to.name")
            echo "  ✅ 리뷰: $REVIEW_STATUS (ID: $REVIEW_ID)"
          else
            echo "  ⚠️  리뷰 transition을 찾을 수 없습니다"
          fi
          echo ""

          # 4단계: 블로킹 transition 찾기 (초기 transitions에서)
          echo "📍 4단계: 블로킹 transition 찾기"
          BLOCKED_ID=$(echo "$INITIAL_TRANSITIONS" | jq -r '.[] | select(
            (.to.name | ascii_downcase) | contains("block") or
            (.to.name | ascii_downcase) | contains("블로킹")
          ) | .id' | head -n 1)

          if [ -n "$BLOCKED_ID" ] && [ "$BLOCKED_ID" != "null" ]; then
            BLOCKED_STATUS=$(echo "$INITIAL_TRANSITIONS" | jq -r ".[] | select(.id == \"$BLOCKED_ID\") | .to.name")
            echo "  ✅ 블로킹: $BLOCKED_STATUS (ID: $BLOCKED_ID)"
          else
            echo "  ⚠️  블로킹 transition을 찾을 수 없습니다"
          fi

          echo "start_id=$START_ID" >> $GITHUB_OUTPUT
          echo "end_id=$END_ID" >> $GITHUB_OUTPUT
          echo "review_id=$REVIEW_ID" >> $GITHUB_OUTPUT
          echo "blocked_id=$BLOCKED_ID" >> $GITHUB_OUTPUT
          echo "start_status=$START_STATUS" >> $GITHUB_OUTPUT
          echo "end_status=$END_STATUS" >> $GITHUB_OUTPUT
          echo "start_trans_name=$START_TRANS_NAME" >> $GITHUB_OUTPUT
          echo "end_trans_name=$END_TRANS_NAME" >> $GITHUB_OUTPUT

      - name: Create workflow config file
        run: |
          SAMPLE_ISSUE="${{ steps.fetch-transitions.outputs.sample_issue }}"
          PROJECT_KEY="${{ secrets.JIRA_PROJECT }}"

          # Multiline output을 heredoc으로 읽기
          TRANSITIONS=$(cat <<'TRANSITIONS_EOF'
          ${{ steps.fetch-transitions.outputs.transitions }}
          TRANSITIONS_EOF
          )

          START_ID="${{ steps.auto-detect.outputs.start_id }}"
          END_ID="${{ steps.auto-detect.outputs.end_id }}"
          REVIEW_ID="${{ steps.auto-detect.outputs.review_id }}"
          BLOCKED_ID="${{ steps.auto-detect.outputs.blocked_id }}"

          START_STATUS="${{ steps.auto-detect.outputs.start_status }}"
          END_STATUS="${{ steps.auto-detect.outputs.end_status }}"
          START_TRANS_NAME="${{ steps.auto-detect.outputs.start_trans_name }}"
          END_TRANS_NAME="${{ steps.auto-detect.outputs.end_trans_name }}"

          # null 또는 빈 값 처리
          [ -z "$START_ID" ] || [ "$START_ID" = "null" ] && START_ID=""
          [ -z "$END_ID" ] || [ "$END_ID" = "null" ] && END_ID=""
          [ -z "$REVIEW_ID" ] || [ "$REVIEW_ID" = "null" ] && REVIEW_ID=""
          [ -z "$BLOCKED_ID" ] || [ "$BLOCKED_ID" = "null" ] && BLOCKED_ID=""

          # Transitions 배열을 정리된 형태로 변환
          TRANSITIONS_JSON=$(echo "$TRANSITIONS" | jq -c 'map({
            id: .id,
            name: .name,
            toStatus: .to.name,
            toStatusId: .to.id
          })')

          cat > scripts/jira/jira-workflow-config.json << EOF
          {
            "lastUpdated": "$(date -u +"%Y-%m-%dT%H:%M:%SZ")",
            "projectKey": "$PROJECT_KEY",
            "sampleIssue": "$SAMPLE_ISSUE",
            "jiraBaseUrl": "${{ secrets.JIRA_BASE_URL }}",
            "transitions": $TRANSITIONS_JSON,
            "mappings": {
              "start": $([ -n "$START_ID" ] && echo "\"$START_ID\"" || echo "null"),
              "end": $([ -n "$END_ID" ] && echo "\"$END_ID\"" || echo "null"),
              "inProgress": $([ -n "$START_ID" ] && echo "\"$START_ID\"" || echo "null"),
              "review": $([ -n "$REVIEW_ID" ] && echo "\"$REVIEW_ID\"" || echo "null"),
              "blocked": $([ -n "$BLOCKED_ID" ] && echo "\"$BLOCKED_ID\"" || echo "null")
            },
            "targetStatuses": {
              "start": "$START_STATUS",
              "end": "$END_STATUS"
            },
            "transitionNames": {
              "start": "$START_TRANS_NAME",
              "end": "$END_TRANS_NAME"
            }
          }
          EOF

          echo "✅ 설정 파일 생성 완료"
          echo ""
          echo "📄 저장된 설정:"
          cat scripts/jira/jira-workflow-config.json | jq '.'

      - name: Commit and push config
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add scripts/jira/jira-workflow-config.json

          if git diff --cached --quiet; then
            echo "⚠️  변경사항 없음"
          else
            COMMIT_MSG=$(cat <<EOF
          chore: Update Jira workflow configuration

          Project: ${{ secrets.JIRA_PROJECT }}
          Sample issue: ${{ steps.fetch-transitions.outputs.sample_issue }}

          Configured transitions:
          - Start: ${{ steps.auto-detect.outputs.start_id }}
          - End: ${{ steps.auto-detect.outputs.end_id }}
          - Review: ${{ steps.auto-detect.outputs.review_id }}
          - Blocked: ${{ steps.auto-detect.outputs.blocked_id }}
          EOF
          )
            
            git commit -m "$COMMIT_MSG"
            git push
            echo "✅ 설정 파일이 커밋되었습니다!"
          fi

      - name: Create summary comment
        uses: actions-cool/issues-helper@v3
        with:
          actions: "create-issue"
          token: ${{ secrets.GITHUB_TOKEN }}
          title: "✅ Jira Workflow 설정 완료"
          body: |
            ## 🎉 Jira Workflow 설정이 완료되었습니다!

            **프로젝트**: ${{ secrets.JIRA_PROJECT }}
            **샘플 이슈**: ${{ steps.fetch-transitions.outputs.sample_issue }}
            **설정 시간**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")

            ### 📋 설정된 Transition

            | 액션 | Transition ID | 상태 |
            |------|--------------|------|
            | 작업 시작 | `${{ steps.auto-detect.outputs.start_id }}` | ${{ steps.auto-detect.outputs.start_id != '' && '✅' || '⚠️ 미설정' }} |
            | 작업 완료 | `${{ steps.auto-detect.outputs.end_id }}` | ${{ steps.auto-detect.outputs.end_id != '' && '✅' || '⚠️ 미설정' }} |
            | 리뷰 | `${{ steps.auto-detect.outputs.review_id }}` | ${{ steps.auto-detect.outputs.review_id != '' && '✅' || '⚠️ 미설정' }} |
            | 블로킹 | `${{ steps.auto-detect.outputs.blocked_id }}` | ${{ steps.auto-detect.outputs.blocked_id != '' && '✅' || '⚠️ 미설정' }} |

            ### 📖 사용 가능한 Transition 목록

            ```json
            ${{ steps.fetch-transitions.outputs.transitions }}
            ```

            ### 💡 다음 단계

            - 설정이 올바른지 확인하세요
            - `npm run issue:start`, `npm run issue:end` 명령어가 이제 이 설정을 사용합니다
            - 수정이 필요하면 Actions 탭에서 "Setup Jira Workflow"를 다시 실행하세요

            **설정 파일**: `scripts/jira/jira-workflow-config.json`
          labels: "jira,automation,config"

      - name: Cleanup temporary issue
        if: steps.fetch-transitions.outputs.should_delete == 'true'
        run: |
          SAMPLE_ISSUE="${{ steps.fetch-transitions.outputs.sample_issue }}"
          echo "🗑️  임시 이슈 삭제 중: $SAMPLE_ISSUE"

          DELETE_RESPONSE=$(curl -s -w "\n%{http_code}" -X DELETE \
            -H "Content-Type: application/json" \
            -u "${{ secrets.JIRA_USER_EMAIL }}:${{ secrets.JIRA_API_TOKEN }}" \
            "${{ secrets.JIRA_BASE_URL }}/rest/api/2/issue/${SAMPLE_ISSUE}")

          HTTP_CODE=$(echo "$DELETE_RESPONSE" | tail -n1)
          RESPONSE_BODY=$(echo "$DELETE_RESPONSE" | sed '$d')

          if [ "$HTTP_CODE" = "204" ] || [ "$HTTP_CODE" = "200" ]; then
            echo "✅ 임시 이슈가 삭제되었습니다"
          else
            echo "⚠️  임시 이슈 삭제 실패 (HTTP $HTTP_CODE)"
            echo "$RESPONSE_BODY"
            echo "💡 권한 문제로 삭제되지 않았지만, workflow 설정은 완료되었습니다."
          fi
